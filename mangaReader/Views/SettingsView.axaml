<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:mangaReader.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
             x:Class="mangaReader.Views.SettingsView"
             x:DataType="vm:SettingsViewModel">

    <Grid RowDefinitions="*">
        <ScrollViewer>
            <StackPanel Margin="30" Spacing="25" MaxWidth="500">
                <TextBlock Text="Configurações" FontSize="28" FontWeight="Bold" Foreground="#2E2E2E"/>
                
                <Separator Height="1" Background="#E0E0E0"/>
                
                <StackPanel Spacing="15">
                    <TextBlock Text="Conta" FontSize="18" FontWeight="SemiBold" Foreground="#424242"/>
                    
                    <StackPanel Spacing="8">
                        <TextBlock Text="Nome de usuário" FontSize="12"/>
                        <TextBlock Text="{Binding CurrentUser.Username}" Padding="10" Height="40" 
                                 Foreground="Black"/>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Email" FontSize="12" Foreground="#616161"/>
                        <TextBlock Text="{Binding CurrentUser.Email}" Padding="10" Height="40" 
                                 Foreground="Black"/>
                    </StackPanel>
                </StackPanel>

                <Separator Height="1" Background="#E0E0E0"/>

                <StackPanel Spacing="15">
                    <TextBlock Text="Trocar senha" FontSize="18" FontWeight="SemiBold" Foreground="#424242"/>
                    
                    <StackPanel Spacing="8">
                        <TextBlock Text="Nova senha" FontSize="12" Foreground="#616161"/>
                        <TextBox Text="{Binding NewPassword}" PasswordChar="•" Padding="10" Height="40" 
                                 Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="1"/>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Confirme a senha" FontSize="12" Foreground="#616161"/>
                        <TextBox Text="{Binding ConfirmPassword}" PasswordChar="•" Padding="10" Height="40" 
                                 Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="1"/>
                    </StackPanel>

                    <Button Content="Mudar" HorizontalContentAlignment="Center" VerticalAlignment="Center" Height="34"
                            Background="#FF6F00" Foreground="White" FontSize="14" FontWeight="Bold"/>
                </StackPanel>

                <Separator Height="1" Background="#E0E0E0"/>

                <StackPanel Spacing="15">
                    <Button Content="Logout" HorizontalContentAlignment="Center" VerticalAlignment="Center" Height="34"
                            Background="#C62828" Foreground="White" FontSize="14" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</UserControl>
